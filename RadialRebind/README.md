# RadialRebind

Adds keybind for the radial menu in BepInEx Configuration Manager.

## Instructions

On initial load, the keybind for the radial menu will load with the default, G. On changing the key bind, the new
key will be set.